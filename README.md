Encapsulation: Data like names and grades are protected within classes.
Inheritance: Student and Teacher inherit from the base class Person.
Polymorphism: The same function describe() behaves differently for each object.
Abstraction: The Person class defines a common structure but is not directly used.
